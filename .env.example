# Path to Google Cloud service account JSON key
GOOGLE_CREDS_PATH=./google_creds.json

# Google Sheet ID (auto-filled by: python3 setup.py init)
SHEET_ID=

# Gmail app password (optional -- only if NOT using Google Apps Script for emails)
GMAIL_APP_PASSWORD=
